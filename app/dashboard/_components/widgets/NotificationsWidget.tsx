"use client";

export default function NotificationsWidget() {
    return (
        <div
            className="h-full rounded-xl p-3 text-sm"
            style={{ border: "1px solid var(--tt-accent)", background: "#111", color: "var(--tt-accent)" }}
        >
            <div className="font-medium mb-1" style={{ color: "#E9CC6A" }}>
                Notifikationscenter
            </div>
            <div>Placeholder – ingen UI endnu.</div>
        </div>
    );
}
