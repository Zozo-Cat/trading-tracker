"use client";

export default function NewsPage() {
    return (
        <main className="p-10 text-center text-lg" style={{ color: "var(--tt-accent)" }}>
            Denne side er flyttet til <b>News Tracker</b>.
        </main>
    );
}
